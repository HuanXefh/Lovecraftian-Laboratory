/*
  ========================================
  Section: Definition
  ========================================
*/


    const BLK_B_materialBlock = require("lovec/temp/blk/BLK_materialBlock");


/*
  ========================================
  Section: Application
  ========================================
*/


    /* <------------------------------ pay0mat ------------------------------> */


    const pay0mat_woodenBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-wooden-block",
    );


    const pay0mat_copperBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-copper-block",
    );


    const pay0mat_leadBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-lead-block",
    );


    const pay0mat_castIronBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-cast-iron-block",
    );


    const pay0mat_scrapSteelBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-scrap-steel-block",
    );


    const pay0mat_steelBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-steel-block",
    );


    const pay0mat_mangalloyBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-mangalloy-block",
    );


    const pay0mat_rubberBlock = extendBlock(
        BLK_B_materialBlock, "pay0mat-rubber-block",
    );


    const pay0mat_brickClay = extendBlock(
        BLK_B_materialBlock, "pay0mat-brick-clay",
    );


    const pay0mat_brickMagnesia = extendBlock(
        BLK_B_materialBlock, "pay0mat-brick-magnesia",
    );


    const pay0mat_brickHighAlumina = extendBlock(
        BLK_B_materialBlock, "pay0mat-brick-high-alumina",
    );


    const pay0mat_brickMullite = extendBlock(
        BLK_B_materialBlock, "pay0mat-brick-mullite",
    );


    const pay0mat_brickSilica = extendBlock(
        BLK_B_materialBlock, "pay0mat-brick-silica",
    );


    const pay0mat_brickCarbon = extendBlock(
      BLK_B_materialBlock, "pay0mat-brick-carbon",
    );
